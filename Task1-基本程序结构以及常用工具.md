> **填写说明**  
> - 队长填写“学习目标、内容、考核方式”等固定部分。  
> - 新生在 `<!-- 新生填写 -->` 区域内完成笔记、练习与总结。  
> - 日期格式统一为 `YYYY-MM-DD`。  

---

## 基本信息

| 项目       | 内容                            |
|------------|----------------------------------|
| **任务编号** | Task-1 |
| **任务名称** | Python 基础——基本程序结构以及常用工具 |
| **提交人**   | <!-- 新生填写姓名 --> |
| **方向**     | <!-- 视觉 / 电控 --> |
| **任内时间** | 2026-05-18 到 2026-05-21 |

---

## 学习目标

1. 掌握Git的基本操作，包括创建仓库、提交代码、拉取代码等。
2. 掌握程序三大基础结构，熟悉Python的基本语法。
3. 学会使用Markdown格式编写笔记。

## 练习任务

### 1. 猜数游戏

#### 随机数生成规则

程序启动后，根据用户指定的区间自动生成一个随机数。**精度由区间端点的格式决定**：

| 区间示例 | 生成规则 |
|---|---|
| `1 - 100`（整数） | 生成随机**整数** |
| `1.00 - 100.00`（两位小数） | 生成随机**两位浮点数** |
| `1.000 - 1000.000`（三位小数） | 生成随机**三位浮点数** |
| `1.000 - 100.0`（最小值三位、最大值一位） | **以最小值精度为准**，生成随机**三位浮点数** |

简单来说：看区间的最小值有几位小数，就生成几位小数的随机数；如果最小值没有小数点，就生成整数。

#### 游戏流程

1. 设定一个最大重试次数。
2. 玩家输入猜测值：
   - 若**猜大了** → 提示"太大了"，告知剩余重试次数，让玩家重新输入。
   - 若**猜小了** → 提示"太小了"，告知剩余重试次数，让玩家重新输入。
   - 若**猜对了** → 提示"恭喜你猜对了！"，并告知总共尝试了多少次。
3. 若重试次数用完仍未猜中 → 提示"游戏结束"，并公布正确答案。
4. 每轮结束后，玩家可以选择**重新开始**，此时随机数重新生成，重试次数重置。
5. **历史记录**：记录玩家本轮每一次的猜测值。猜对或游戏结束时，展示本轮所有猜测轨迹，例如：`你的猜测轨迹：50 ↑ → 80 ↓ → 65 ↑ → 72 ↓ → 67 √`（用 ↑ 表示上次猜小了、↓ 表示上次猜大了），让玩家直观看到自己是如何一步步逼近答案的。

可以适当添加一些额外的功能，例如：让玩家设定范围、添加倒计时等等。自行添加描述输出，如：“请输入你猜的数字（例如：100）：:”。

#### 提交要求

**1. 仓库创建**

使用 Git 管理代码，在**GitHub**创建一个 Public（公开）仓库。

仓库名称为：`whsw-Task{任务编号}-{姓名}`，例如：`whsw-Task1-lijinzhou`

目录结构如下：

```
whsw-Task1-{姓名}/
├── Task1-基本程序结构以及常用工具/
│   ├── Task1-基本程序结构以及常用工具.md    # 本任务说明文档
│   ├── 笔记/                               # 学习笔记文件夹
│   └── code/
│       └── main.py                         # 猜数游戏代码
├── Task2-.../
```

**2. 学习笔记**

使用 **Markdown** 格式整理自己的学习笔记，内容可以包括：
- 本次任务中学到的新知识点
- 遇到的问题及解决方法
- 代码思路和关键逻辑的说明

笔记文件和图片等资源统一放入 `笔记/` 文件夹，**不允许手写后拍照提交**，必须使用 Markdown 编辑器编写。

**3. 关于 AI 工具的使用**

- **不允许**使用 AI 直接生成代码。
- **允许**向 AI 提问来帮助解决困难，例如：
  - "如何配置 Git 并提交代码？"
  - "Python 中如何获取一个小数有几位小数？"
  - "while 循环和 for 循环有什么区别？"
- 把 AI 当成**老师**而不是**代写工具**，理解原理后再自己动手写。

---

### 2. 知识点清单

在开始写代码之前，建议先掌握以下知识点：

| 类别 | 知识点 | 在游戏中的应用 |
|---|---|---|
| **顺序结构** | 变量与数据类型、`input()` 输入、`print()` 输出 | 程序的整体执行流程 |
| **分支结构** | `if / elif / else` 条件判断 | 判断猜大了还是猜小了 |
| **循环结构** | `while` 循环、`break` 退出 | 反复让玩家猜测直到猜中或用完次数 |
| **随机数** | `random.randint()`、`random.uniform()` | 生成目标数字 |
| **字符串处理** | `split()`、`len()`、`isdigit()` | 判断小数点位数、校验输入是否合法 |
| **列表** | `list.append()`、遍历列表 | 存储历史猜测记录 |
| **异常处理** | `try / except` | 防止玩家输入非数字导致程序崩溃 |
| **函数** | `def` 定义函数、参数与返回值 | 将游戏逻辑拆分为独立函数（如生成随机数、判断大小） |
